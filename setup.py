# A dummy setup script so we can run toxbootstrap.py on this project itself.
# http://hudson.activestate.com/hudson/job/tox-bootstrap/

from setuptools import setup

setup(name='dummy',
      version=0.1,
      description="Dummy module",
      )
